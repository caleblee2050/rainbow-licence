# Rainbow Licence M1 시연 베타 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 3주 안에 다문화 학교(NEXT SCHOOL) 시연 가능한 Rainbow Licence 베타 완성 — 자격증 3개(한식조리·미용일반·제과) 콘텐츠 풀 충실, 디자인 시스템 V3(Deep Teal + Terracotta) 적용, 마이그레이션 버그·DEMO 모드·Vercel 배포까지.

**Architecture:** Next.js 16 App Router + React 19 + JS. 클라이언트 localStorage 영속화(`rainbow_study` key). 데이터는 `src/data/{licences,questions,terms}.js`. 학습 엔진은 `src/lib/studyEngine.js` (SM-2 간격반복). 추가될 핵심 신규 모듈: `src/lib/demoMode.js`, `src/components/ErrorBoundary.js`. 콘텐츠 병렬 작업을 위해 `questions.js`/`terms.js`를 자격증별 파일로 분리.

**Tech Stack:** Next.js 16.1.6 (Turbopack dev), React 19.2.3, JS (no TS), Vitest (신규 추가), Vercel 배포, Google Fonts (Pretendard CDN + Fraunces + Geist + JetBrains Mono).

**Spec:** `docs/superpowers/specs/2026-05-26-rainbow-licence-beta-design.md`
**Design tokens:** `DESIGN.md`

---

## File Structure

### Files to create
```
src/lib/demoMode.js                    # DEMO 모드/학교명 헬퍼
src/components/ErrorBoundary.js        # React ErrorBoundary
src/data/questions/index.js            # 자격증별 모아 export
src/data/questions/korean-food.js      # 한식조리 60문제
src/data/questions/beauty-general.js   # 미용일반 60문제
src/data/questions/pastry.js           # 제과 60문제 (신규)
src/data/terms/index.js                # 자격증별 모아 export
src/data/terms/korean-food.js          # 한식 40용어
src/data/terms/beauty-general.js       # 미용 40용어
src/data/terms/pastry.js               # 제과 40용어
vitest.config.mjs                      # Vitest 설정
src/lib/__tests__/studyEngine.test.js  # studyEngine 테스트
.env.example                           # 환경변수 템플릿
.env.local                             # 로컬 환경변수 (gitignored)
```

### Files to modify
```
src/lib/studyEngine.js                 # migration fix (default deep-merge)
src/data/questions.js                  # 삭제 후 questions/index.js로 대체 (re-export)
src/data/terms.js                      # 삭제 후 terms/index.js로 대체 (re-export)
src/app/globals.css                    # V2 토큰 → V3(D2 Deep Teal) 토큰
src/app/layout.js                      # 폰트 로드, ErrorBoundary 래핑, 메타태그
src/app/page.js                        # ErrorBoundary 적용
src/components/layout/TopNav.js        # 학교명, 5언어 도트, D2 적용
src/components/layout/BottomNav.js     # D2 적용
src/components/pages/HomePage.js       # D2 적용
src/components/pages/OnboardingPage.js # D2 적용 + 평등 카드
src/components/pages/LicencePage.js    # D2 적용
src/components/pages/StudyPage.js      # D2 적용
src/components/pages/DictionaryPage.js # D2 적용 + 5언어 정책
src/components/pages/CommunityPage.js  # D2 적용
src/components/ui/PremiumBanner.js     # DEMO 모드 분기
package.json                           # vitest, @vitejs/plugin-react devDeps
.gitignore                             # .env.local 추가
```

---

## Task 1: Bootstrap — Vitest + 데이터 파일 분리

**Files:**
- Create: `vitest.config.mjs`
- Create: `src/lib/__tests__/studyEngine.test.js`
- Create: `src/data/questions/index.js`, `src/data/questions/korean-food.js`, `src/data/questions/beauty-general.js`, `src/data/questions/pastry.js`
- Create: `src/data/terms/index.js`, `src/data/terms/korean-food.js`, `src/data/terms/beauty-general.js`, `src/data/terms/pastry.js`
- Modify: `src/data/questions.js`, `src/data/terms.js` (re-export 한 줄로 축소)
- Modify: `package.json` (vitest + plugin react devDeps + test script)

이 작업은 후속 모든 작업의 기반. 데이터 분리하지 않으면 콘텐츠 작업 시 파일 충돌. 테스트 프레임워크 없으면 안정화 작업의 TDD 불가능.

- [ ] **Step 1: Vitest + React 플러그인 설치**

```bash
cd /Volumes/AIPART/dev/rainbow-licence
npm install -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/jest-dom
```

- [ ] **Step 2: `vitest.config.mjs` 작성**

```js
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
    plugins: [react()],
    test: {
        environment: 'jsdom',
        globals: true,
        setupFiles: [],
    },
    resolve: {
        alias: { '@': path.resolve(__dirname, './src') },
    },
});
```

- [ ] **Step 3: `package.json`에 test 스크립트 추가**

scripts 섹션에 추가:
```json
"test": "vitest",
"test:run": "vitest run"
```

- [ ] **Step 4: 스모크 테스트 작성 — 테스트가 실행되는지 확인**

`src/lib/__tests__/studyEngine.test.js`:
```js
import { describe, it, expect, beforeEach } from 'vitest';
import { calculateNextReview } from '../studyEngine';

describe('studyEngine smoke', () => {
    it('calculateNextReview returns card with required fields', () => {
        const card = { repetition: 0, easeFactor: 2.5, interval: 0 };
        const result = calculateNextReview(card, 5);
        expect(result).toHaveProperty('repetition');
        expect(result).toHaveProperty('easeFactor');
        expect(result).toHaveProperty('interval');
        expect(result).toHaveProperty('nextReviewDate');
        expect(result).toHaveProperty('lastReviewed');
    });
});
```

- [ ] **Step 5: 테스트 실행해서 통과 확인**

```bash
npm run test:run
```
Expected: `1 passed`

- [ ] **Step 6: `src/data/questions/` 디렉토리 생성 + 자격증별 파일 분리**

`src/data/questions/korean-food.js`: 현재 `src/data/questions.js`의 `licenceId: 'korean-food'` 문제 15개를 그대로 옮긴다. 파일 헤더:
```js
// 한식조리기능사 기출 변형 문제
export const koreanFoodQuestions = [
    // ... 기존 15개 문제 그대로
];
```

`src/data/questions/beauty-general.js`: `licenceId: 'beauty-general'` 문제 10개를 옮긴다:
```js
export const beautyGeneralQuestions = [
    // ... 기존 10개
];
```

`src/data/questions/pastry.js`: 빈 배열로 시작 (Task 11에서 채움):
```js
// 제과기능사 기출 변형 문제 (Task 11에서 60문제까지 채움)
export const pastryQuestions = [];
```

`src/data/questions/index.js`:
```js
import { koreanFoodQuestions } from './korean-food';
import { beautyGeneralQuestions } from './beauty-general';
import { pastryQuestions } from './pastry';

export const questions = [
    ...koreanFoodQuestions,
    ...beautyGeneralQuestions,
    ...pastryQuestions,
];
```

- [ ] **Step 7: `src/data/questions.js`를 re-export 한 줄로 축소**

```js
export { questions } from './questions/index';
```
이렇게 하면 import `from '@/data/questions'`로 쓰던 기존 코드가 그대로 동작.

- [ ] **Step 8: `src/data/terms/`도 동일하게 분리**

`src/data/terms/korean-food.js`: 기존 `'korean-food'` 키 배열을 옮김:
```js
export const koreanFoodTerms = [
    // ... 23개 기존 용어
];
```

`src/data/terms/beauty-general.js`: 기존 `'beauty-general'` 키 12개:
```js
export const beautyGeneralTerms = [
    // ... 12개
];
```

`src/data/terms/pastry.js`: 빈 배열로 시작:
```js
export const pastryTerms = [];
```

`src/data/terms/index.js`:
```js
import { koreanFoodTerms } from './korean-food';
import { beautyGeneralTerms } from './beauty-general';
import { pastryTerms } from './pastry';

export const terms = {
    'korean-food': koreanFoodTerms,
    'beauty-general': beautyGeneralTerms,
    'pastry': pastryTerms,
};
```

`src/data/terms.js`:
```js
export { terms } from './terms/index';
```

- [ ] **Step 9: 빌드/런타임 검증**

```bash
npm run dev
```
브라우저에서 http://localhost:3001 열어 학습 화면 진입 — 한식조리·미용일반 문제가 그대로 보이는지 확인.

- [ ] **Step 10: 커밋**

```bash
git add vitest.config.mjs package.json package-lock.json src/lib/__tests__ src/data/
git commit -m "chore: vitest setup + 자격증별 데이터 파일 분리

- vitest + @vitejs/plugin-react + jsdom + testing-library 추가
- npm test, npm run test:run 스크립트
- src/data/questions/{korean-food,beauty-general,pastry}.js 분리
- src/data/terms/{korean-food,beauty-general,pastry}.js 분리
- 기존 questions.js / terms.js는 re-export로 축소 (호환 유지)
- studyEngine smoke 테스트 1건 통과 확인"
```

---

## Task 2: 안정화 — Migration fix + ErrorBoundary + demoMode

**Files:**
- Modify: `src/lib/studyEngine.js` (default deep-merge + 빈 데이터 폴백)
- Create: `src/lib/__tests__/studyEngine.test.js` (extend)
- Create: `src/lib/demoMode.js`
- Create: `src/lib/__tests__/demoMode.test.js`
- Create: `src/components/ErrorBoundary.js`
- Create: `.env.example`
- Modify: `.gitignore`

마이그레이션 버그(`isPremium()` crashes when old localStorage missing `premium` key)는 시연에서 절대 일어나면 안 됨. 이걸 TDD로 잡고, ErrorBoundary로 추가 안전망.

- [ ] **Step 1: Migration 버그 재현 테스트 작성 (실패 예상)**

`src/lib/__tests__/studyEngine.test.js`에 추가:
```js
import { isPremium, getStreak, getStats } from '../studyEngine';

describe('studyEngine — legacy localStorage migration', () => {
    beforeEach(() => {
        localStorage.clear();
    });

    it('isPremium() does not crash when premium key missing (legacy data)', () => {
        // 옛 버전 데이터: premium 키 없음
        const legacyData = {
            user: { language: 'vi', onboardingComplete: true },
            streak: { current: 0, longest: 0 },
            cards: {},
            stats: { totalAnswered: 0, totalCorrect: 0, byLicence: {}, bySubject: {}, dailyLog: {} },
            // premium 키 없음
        };
        localStorage.setItem('rainbow_study', JSON.stringify(legacyData));
        expect(() => isPremium()).not.toThrow();
        expect(isPremium()).toBe(false);
    });

    it('getStreak() does not crash with empty localStorage', () => {
        expect(() => getStreak()).not.toThrow();
        expect(getStreak()).toEqual(expect.objectContaining({ current: 0, longest: 0 }));
    });

    it('getStats() returns defaults when stats key missing', () => {
        localStorage.setItem('rainbow_study', JSON.stringify({ user: {} }));
        expect(() => getStats()).not.toThrow();
        expect(getStats()).toEqual(expect.objectContaining({
            totalAnswered: 0,
            totalCorrect: 0,
        }));
    });
});
```

- [ ] **Step 2: 테스트 실행해서 실패 확인**

```bash
npm run test:run
```
Expected: `isPremium()` 테스트 FAIL with `Cannot read properties of undefined (reading 'active')`

- [ ] **Step 3: `getStudyData()`에 default deep-merge 적용**

`src/lib/studyEngine.js`의 `getStudyData()` 함수 수정 (현재 line 39-47):
```js
function deepMerge(target, source) {
    // 단순 deep merge: target에 source의 누락된 키를 채워넣음
    const result = { ...target };
    for (const key of Object.keys(target)) {
        if (
            target[key] !== null &&
            typeof target[key] === 'object' &&
            !Array.isArray(target[key]) &&
            source[key] !== null &&
            typeof source[key] === 'object' &&
            !Array.isArray(source[key])
        ) {
            result[key] = deepMerge(target[key], source[key]);
        } else if (source[key] !== undefined) {
            result[key] = source[key];
        }
    }
    return result;
}

function getStudyData() {
    if (typeof window === 'undefined') return getDefaultData();
    try {
        const raw = localStorage.getItem(STORAGE_KEY);
        if (!raw) return getDefaultData();
        const stored = JSON.parse(raw);
        // Migration: 누락된 키를 default에서 채움 (기존 데이터는 유지)
        return deepMerge(getDefaultData(), stored);
    } catch {
        return getDefaultData();
    }
}
```

- [ ] **Step 4: 테스트 재실행해서 통과 확인**

```bash
npm run test:run
```
Expected: 모든 테스트 PASS

- [ ] **Step 5: `src/lib/demoMode.js` 작성**

```js
// 데모/학교 환경 감지 + 학교명 노출 헬퍼
const DEMO_FLAG = process.env.NEXT_PUBLIC_DEMO_MODE === 'true';
const SCHOOL = process.env.NEXT_PUBLIC_SCHOOL_NAME || '';

export function isDemoMode() {
    return DEMO_FLAG;
}

export function getSchoolName() {
    return SCHOOL;
}

export function shouldHidePremium() {
    return isDemoMode();
}

export function isPremiumGateUnlocked() {
    // DEMO 모드에선 모든 프리미엄 기능 잠금 해제
    return isDemoMode();
}
```

- [ ] **Step 6: `demoMode` 테스트**

`src/lib/__tests__/demoMode.test.js`:
```js
import { describe, it, expect } from 'vitest';
import { isDemoMode, getSchoolName, shouldHidePremium, isPremiumGateUnlocked } from '../demoMode';

describe('demoMode', () => {
    it('reads env flags via the module', () => {
        // 정적 환경변수 확인 — process.env.NEXT_PUBLIC_DEMO_MODE은
        // 빌드 타임에 인라이닝되므로 vitest에선 module-level 상수 확인
        expect(typeof isDemoMode()).toBe('boolean');
        expect(typeof getSchoolName()).toBe('string');
    });
    it('shouldHidePremium and isPremiumGateUnlocked agree with isDemoMode', () => {
        expect(shouldHidePremium()).toBe(isDemoMode());
        expect(isPremiumGateUnlocked()).toBe(isDemoMode());
    });
});
```

```bash
npm run test:run
```
Expected: PASS

- [ ] **Step 7: `src/components/ErrorBoundary.js` 작성**

```jsx
'use client';
import React from 'react';

export default class ErrorBoundary extends React.Component {
    constructor(props) {
        super(props);
        this.state = { hasError: false, error: null };
    }
    static getDerivedStateFromError(error) {
        return { hasError: true, error };
    }
    componentDidCatch(error, info) {
        // 개발 시 콘솔에 남김
        console.error('[ErrorBoundary]', error, info);
    }
    handleReset = () => {
        this.setState({ hasError: false, error: null });
    };
    render() {
        if (this.state.hasError) {
            return (
                <div style={{
                    padding: 'var(--space-lg, 24px)',
                    margin: 'var(--space-lg, 24px)',
                    border: '1px solid var(--border, #E5DED2)',
                    borderRadius: 'var(--radius-md, 8px)',
                    background: 'var(--surface, #FFFFFF)',
                    color: 'var(--text-primary, #1E1E1E)',
                    fontFamily: 'var(--font-body, system-ui)',
                }}>
                    <h2 style={{ marginTop: 0, fontSize: 18 }}>⚠️ 일시적인 문제가 발생했어요</h2>
                    <p style={{ fontSize: 14, color: 'var(--text-secondary, #5A5048)' }}>
                        화면을 다시 불러와 주세요. 문제가 계속되면 데모 초기화를 시도해 보세요.
                    </p>
                    <button
                        onClick={this.handleReset}
                        style={{
                            marginTop: 12,
                            padding: '8px 16px',
                            background: 'var(--primary, #0B5563)',
                            color: '#fff',
                            border: 'none',
                            borderRadius: 'var(--radius-sm, 6px)',
                            cursor: 'pointer',
                        }}
                    >
                        다시 시도
                    </button>
                </div>
            );
        }
        return this.props.children;
    }
}
```

- [ ] **Step 8: `.env.example` 작성 + `.gitignore` 업데이트**

`.env.example`:
```
NEXT_PUBLIC_DEMO_MODE=true
NEXT_PUBLIC_SCHOOL_NAME=NEXT SCHOOL
```

`.gitignore`에 추가 (없으면):
```
.env.local
.env*.local
```

`.env.local` 작성 (개발용 — gitignored):
```
NEXT_PUBLIC_DEMO_MODE=true
NEXT_PUBLIC_SCHOOL_NAME=NEXT SCHOOL
```

- [ ] **Step 9: 브라우저에서 검증**

```bash
npm run dev
```
브라우저에서 옛 데이터로 시나리오 재현:
1. F12 콘솔에서 `localStorage.setItem('rainbow_study', JSON.stringify({user:{onboardingComplete:true}, stats:{}}))`
2. 새로고침 → 앱이 죽지 않고 홈 페이지 로드되는지 확인

- [ ] **Step 10: 커밋**

```bash
git add src/lib/studyEngine.js src/lib/demoMode.js src/lib/__tests__ src/components/ErrorBoundary.js .env.example .env.local .gitignore
git commit -m "fix: localStorage migration + ErrorBoundary + demoMode 헬퍼

- studyEngine: getStudyData()에 default deep-merge — 옛 localStorage(premium 키 없음)에서
  isPremium() crash 막음
- demoMode: NEXT_PUBLIC_DEMO_MODE, NEXT_PUBLIC_SCHOOL_NAME 헬퍼
- ErrorBoundary 컴포넌트 — 시연 중 한 컴포넌트 터져도 전체 안 죽게
- 6개 테스트 케이스 추가, 전부 통과"
```

---

## Task 3: Design Tokens V3 (globals.css 치환 + 폰트)

**Files:**
- Modify: `src/app/globals.css` (전체 :root 블록 + 폰트 import 교체)
- Modify: `src/app/layout.js` (폰트 preconnect 추가)

`DESIGN.md` 토큰을 그대로 CSS variable로 옮긴다. V2 변수명을 V3로 변경하되, 컴포넌트들이 참조하는 이름은 그대로 유지 (—primary, —accent 등) 해서 컴포넌트 작업 분리 가능.

- [ ] **Step 1: `src/app/globals.css` 상단 import 블록 교체**

기존:
```css
@import url('https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@300;400;500;600;700;800;900&display=swap');
```

→ 다음으로 교체:
```css
@import url('https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable.min.css');
@import url('https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600;9..144,700&family=Geist:wght@400;500;600&family=JetBrains+Mono:wght@400;500&display=swap');
```

- [ ] **Step 2: `:root` 블록 전체 V3 토큰으로 교체**

기존 :root 블록 (line ~10 ~ ~160 추정 — 첫 번째 `}` 닫힘까지) 전체를 다음으로 교체:

```css
:root {
    /* === V3 Design Tokens — Deep Teal + Terracotta === */

    /* Primary (Deep Teal) */
    --primary: #0B5563;
    --primary-light: #16798C;
    --primary-dark: #073F4A;
    --primary-soft: rgba(11, 85, 99, 0.08);
    --primary-50: rgba(11, 85, 99, 0.05);
    --primary-100: rgba(11, 85, 99, 0.10);
    --primary-200: rgba(11, 85, 99, 0.20);

    /* Accent (Warm Terracotta) */
    --accent: #C25A3F;
    --accent-light: #D9785E;
    --accent-soft: #E8C4B5;

    /* Background / Surface */
    --bg-main: #F5EFE6;
    --bg-card: #FFFFFF;
    --bg-elevated: #FFFFFF;
    --bg-overlay: rgba(30, 30, 30, 0.5);

    /* Border */
    --border: #E5DED2;
    --border-strong: #C9BFB0;

    /* Text */
    --text-primary: #1E1E1E;
    --text-secondary: #5A5048;
    --text-muted: #8A7E70;
    --text-inverse: #FFFFFF;

    /* Semantic */
    --success: #2D7A39;
    --success-bg: rgba(45, 122, 57, 0.08);
    --warning: #C77700;
    --warning-bg: rgba(199, 119, 0, 0.10);
    --error: #B23A2E;
    --error-bg: rgba(178, 58, 46, 0.08);
    --info: #1565C0;
    --info-bg: rgba(21, 101, 192, 0.08);

    /* Neutrals (호환용 — 점진 제거 가능) */
    --white: #FFFFFF;
    --gray-50: #FAF6EF;
    --gray-100: #F1ECE2;
    --gray-200: #E5DED2;
    --gray-300: #C9BFB0;
    --gray-400: #A99E8E;
    --gray-500: #8A7E70;
    --gray-600: #6B6055;
    --gray-700: #4A4239;
    --gray-800: #2D2820;
    --gray-900: #1E1E1E;

    /* Shadows (warm-toned) */
    --shadow-xs: 0 1px 2px rgba(30, 30, 30, 0.04);
    --shadow-sm: 0 2px 4px rgba(30, 30, 30, 0.06), 0 1px 2px rgba(30, 30, 30, 0.04);
    --shadow-md: 0 4px 12px rgba(30, 30, 30, 0.08), 0 2px 4px rgba(30, 30, 30, 0.04);
    --shadow-lg: 0 8px 20px rgba(30, 30, 30, 0.10), 0 4px 8px rgba(30, 30, 30, 0.04);
    --shadow-card: 0 2px 6px rgba(30, 30, 30, 0.05), 0 0 1px rgba(30, 30, 30, 0.08);
    --shadow-card-hover: 0 6px 16px rgba(30, 30, 30, 0.10), 0 0 1px rgba(30, 30, 30, 0.10);

    /* Radius (V2 24-32px → V3 8-12px) */
    --radius-xs: 4px;
    --radius-sm: 6px;
    --radius-md: 8px;
    --radius-lg: 12px;
    --radius-xl: 16px;
    --radius-full: 9999px;

    /* Spacing — 8px base */
    --space-2xs: 2px;
    --space-xs: 4px;
    --space-sm: 8px;
    --space-md: 16px;
    --space-lg: 24px;
    --space-xl: 32px;
    --space-2xl: 48px;
    --space-3xl: 64px;

    /* Typography */
    --font-display: 'Fraunces', 'Pretendard Variable', Georgia, serif;
    --font-body: 'Pretendard Variable', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    --font-data: 'Geist', 'Pretendard Variable', monospace;
    --font-mono: 'JetBrains Mono', 'Geist Mono', monospace;
    --font-family: var(--font-body); /* 호환 */

    /* Motion */
    --easing-enter: cubic-bezier(0, 0, 0.2, 1);
    --easing-exit: cubic-bezier(0.4, 0, 1, 1);
    --easing-move: cubic-bezier(0.4, 0, 0.2, 1);
    --dur-micro: 120ms;
    --dur-short: 220ms;
    --dur-medium: 320ms;

    /* 5-Language Identity (디테일용, 풀그라데이션 ❌) */
    --lang-vi: #C25A3F;
    --lang-zh: #B91C1C;
    --lang-th: #1F6F8B;
    --lang-tl: #2D7A39;
    --lang-my: #6B4226;
}
```

- [ ] **Step 3: 무지개·보라 그라데이션 정의 제거 (slop 제거)**

기존 globals.css에서 `--gradient-rainbow`, `--gradient-primary`(`#667EEA → #764BA2`), `--gradient-warm`(`#F093FB → #F5576C`), `--gradient-cool`을 검색해서 다음과 같이 교체:

```css
/* V3 Gradients — restrained */
--gradient-primary: linear-gradient(135deg, #0B5563 0%, #073F4A 100%);
--gradient-accent: linear-gradient(135deg, #C25A3F 0%, #A04A30 100%);
--gradient-card: linear-gradient(135deg, rgba(255, 255, 255, 0.95) 0%, rgba(255, 255, 255, 0.85) 100%);
/* --gradient-rainbow, --gradient-warm, --gradient-cool 완전 제거 */
```

- [ ] **Step 4: body 폰트 적용 확인**

`body` 또는 `html` 셀렉터의 `font-family`가 `var(--font-body)`를 가리키는지 확인. 없으면 추가:
```css
body {
    font-family: var(--font-body);
    background: var(--bg-main);
    color: var(--text-primary);
}
```

- [ ] **Step 5: `src/app/layout.js`에 폰트 preconnect 추가**

`<head>` 또는 Next.js 메타데이터에 추가:
```jsx
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
<link rel="preconnect" href="https://cdn.jsdelivr.net" />
```

- [ ] **Step 6: 브라우저에서 시각 검증**

```bash
npm run dev
```
홈 화면 들어가서:
1. 폰트가 Pretendard로 보이는지 (Noto Sans KR 아님)
2. 배경이 따뜻한 모래색(`#F5EFE6`)인지 (기존 라일락 `#F0F2FF` 아님)
3. 보라 그라데이션이 사라졌는지 (홈 상단 hero가 딥 틸로)

이 시점에선 컴포넌트별 색상이 V2 유산이라 일관성 깨질 수 있음 — Task 4-8에서 컴포넌트별 처리.

- [ ] **Step 7: 커밋**

```bash
git add src/app/globals.css src/app/layout.js
git commit -m "feat: V3 design tokens — Deep Teal + Terracotta

- :root 토큰 전면 교체 (DESIGN.md 기준)
- 폰트: Noto Sans KR → Pretendard Variable + Fraunces + Geist + JetBrains Mono
- 무지개·보라 그라데이션 정의 제거 (AI slop 안티패턴)
- radius 24-32px → 8-12px (토이 느낌 제거)
- 배경 차가운 라일락 → 따뜻한 모래 #F5EFE6
- 5색은 디테일용 변수로만 유지 (--lang-*)"
```

---

## Task 4: Layout 컴포넌트 — TopNav + BottomNav

**Files:**
- Modify: `src/components/layout/TopNav.js` (학교명, 5언어 도트, D2 적용)
- Modify: `src/components/layout/BottomNav.js` (D2 적용)
- Read: `src/lib/demoMode.js` (Task 2에서 이미 작성됨)

학교명 디테일 + 5언어 도트는 디자인 차별 포인트. TopNav 작업 후 BottomNav 마무리.

- [ ] **Step 1: 현재 TopNav 코드 읽고 구조 파악**

```bash
cat src/components/layout/TopNav.js
```
브랜드 워드마크, 언어 선택, 가능하면 우측 사용자 영역이 있을 것.

- [ ] **Step 2: TopNav에서 학교명 표시 추가**

브랜드 워드마크 옆 또는 아래에 학교명 작게:
```jsx
import { getSchoolName } from '@/lib/demoMode';

// 컴포넌트 안에서:
const schoolName = getSchoolName();

// 워드마크 옆:
<div className="brand">
    <span className="brand-mark">🌈 Rainbow Licence</span>
    {schoolName && <span className="school-name">{schoolName}</span>}
</div>
```

CSS:
```css
.brand-mark {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 18px;
    color: var(--primary);
}
.school-name {
    display: block;
    font-size: 11px;
    color: var(--text-muted);
    letter-spacing: 0.5px;
    text-transform: uppercase;
    margin-top: 2px;
}
```

- [ ] **Step 3: 언어 선택 픽 아래에 5언어 도트 추가**

언어 선택 픽(예: 🇻🇳 Tiếng Việt) 아래에 5개 도트:
```jsx
<div className="lang-picker-wrap">
    {/* 기존 언어 픽 버튼 */}
    <button className="lang-pick">{flag} {label}</button>
    <div className="lang-dots" aria-hidden="true">
        <span style={{background:'var(--lang-vi)'}} />
        <span style={{background:'var(--lang-zh)'}} />
        <span style={{background:'var(--lang-th)'}} />
        <span style={{background:'var(--lang-tl)'}} />
        <span style={{background:'var(--lang-my)'}} />
    </div>
</div>
```

CSS:
```css
.lang-dots {
    display: flex;
    gap: 4px;
    margin-top: 4px;
    justify-content: center;
}
.lang-dots span {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    opacity: 0.7;
}
```

- [ ] **Step 4: TopNav 색상·라운드 V3 적용**

기존 보라 계열 컬러(`#5C7CFA`, `#667EEA` 등 하드코딩) 검색해서 모두 `var(--primary)`로 치환. radius도 24px+ 발견 시 16px 이하로 축소.

- [ ] **Step 5: BottomNav도 V3 적용**

```bash
cat src/components/layout/BottomNav.js
```
- 활성 탭 색상을 `var(--primary)`로
- 배경 `var(--bg-card)`, 상단 border `1px solid var(--border)`
- 활성 표시(작은 점/언더라인)는 `var(--accent)`로 (액센트 사용 절제 — 활성 탭에만)

- [ ] **Step 6: 브라우저에서 검증**

```bash
npm run dev
```
모바일 폭으로:
1. TopNav에 "NEXT SCHOOL" 보이는지
2. 언어 픽 아래 5개 도트 보이는지 (각각 다른 색)
3. BottomNav 활성 탭이 딥 틸인지

데스크탑 폭(1280+)에서 사이드바도 동일하게 V3 적용됐는지 확인.

- [ ] **Step 7: 커밋**

```bash
git add src/components/layout/
git commit -m "feat: TopNav/BottomNav V3 적용 + 학교명·5언어 도트

- TopNav: NEXT SCHOOL 학교명 워드마크 아래 작게 표시 (getSchoolName)
- TopNav: 언어 픽 아래 5개 언어 색상 도트 — 다양성 시각적 자랑
- BottomNav: 활성 탭 딥 틸, 활성 표시 테라코타
- 보라 하드코딩 컬러를 --primary 변수로 치환
- radius 16px 이하 축소"
```

---

## Task 5: HomePage + PremiumBanner DEMO 분기

**Files:**
- Modify: `src/components/pages/HomePage.js`
- Modify: `src/components/ui/PremiumBanner.js`

HomePage는 데모/평가위원이 처음 보는 화면. PremiumBanner를 DEMO 모드에선 "기관 도입 문의"로 바꿔 결제 노이즈 제거.

- [ ] **Step 1: HomePage 색상·그라데이션 V3 적용**

```bash
cat src/components/pages/HomePage.js | head -100
```
Hero 섹션의 `background: 'var(--gradient-primary)'` 그대로 두지만, 그라데이션이 Task 3에서 이미 딥 틸로 바뀜.

직접 하드코딩된 보라(`#5C7CFA`, `#7950F2`, `#667EEA`, `#764BA2`, `#FF6B9D` 등) 검색해서 `var(--primary)` / `var(--accent)`로 치환.

특히 무지개 그라데이션 사용처(`var(--gradient-rainbow)`)는 모두 제거 — 브랜드 마크 옆 작은 액센트만 유지하고 큰 면적은 단색 또는 `var(--gradient-primary)`로.

- [ ] **Step 2: 통계 4타일 톤 조정**

풀이수·정답률·복습·스트릭 4타일이 무지개 4색을 쓰고 있다면, 4개 모두 `var(--text-primary)` 숫자 + 작은 `var(--accent)` 아이콘으로 절제. 카드 배경은 `var(--bg-card)`.

- [ ] **Step 3: PremiumBanner DEMO 분기**

```bash
cat src/components/ui/PremiumBanner.js
```

기존 banner JSX 위에 분기 추가:
```jsx
'use client';
import { shouldHidePremium } from '@/lib/demoMode';

export default function PremiumBanner(props) {
    if (shouldHidePremium()) {
        return <InstitutionInquiryCard />;
    }
    // 기존 PremiumBanner 렌더링...
}

function InstitutionInquiryCard() {
    return (
        <div style={{
            background: 'var(--bg-card)',
            border: '1px solid var(--border)',
            borderRadius: 'var(--radius-md)',
            padding: 'var(--space-lg)',
            marginBottom: 'var(--space-lg)',
        }}>
            <div style={{ display: 'flex', alignItems: 'center', gap: 'var(--space-sm)' }}>
                <span style={{ fontSize: 28 }}>📋</span>
                <div>
                    <h3 style={{
                        margin: 0,
                        fontFamily: 'var(--font-display)',
                        fontSize: 18,
                        color: 'var(--text-primary)',
                    }}>기관 도입 문의</h3>
                    <p style={{
                        margin: '4px 0 0',
                        fontSize: 13,
                        color: 'var(--text-secondary)',
                    }}>학교·다문화센터·공공기관용 도입 상담</p>
                </div>
            </div>
        </div>
    );
}
```

- [ ] **Step 4: 인기 자격증 카드 V3**

각 자격증 카드의 그라데이션 배경을 단색 + 라운드 md로 변경. icon은 그대로 두되 카드 전체가 무지개색이지 않게.

- [ ] **Step 5: 단계별 학습법 카드 V3**

STEP 0-4 카드의 무지개 색상 띠를 제거. 각 카드는 흰 배경 + 좌측 액센트 바(`var(--accent)` 또는 `var(--primary)`)로 변경.

- [ ] **Step 6: 브라우저 검증**

```bash
npm run dev
```
홈 화면:
1. 보라/무지개 그라데이션 흔적 없는지
2. PremiumBanner 자리에 "기관 도입 문의" 카드 보이는지 (.env.local의 DEMO_MODE=true 덕분)
3. 4타일 통계가 절제된 톤인지
4. 단계별 학습법 5개 카드가 일관된 톤인지

- [ ] **Step 7: 커밋**

```bash
git add src/components/pages/HomePage.js src/components/ui/PremiumBanner.js
git commit -m "feat: HomePage V3 + PremiumBanner DEMO 분기

- HomePage: 보라/무지개 그라데이션 흔적 제거, --primary/--accent 변수로 통일
- PremiumBanner: DEMO 모드에선 '기관 도입 문의' 카드로 교체
- 단계별 학습법 카드 색상 띠 제거, 절제된 톤으로
- 4타일 통계: 무지개 4색 → 절제된 액센트"
```

---

## Task 6: OnboardingPage V3 + 평등 카드

**Files:**
- Modify: `src/components/pages/OnboardingPage.js`

5개 언어 카드를 동등하게 보여서 "주류 없음"의 다양성 정체성 강화.

- [ ] **Step 1: OnboardingPage 색상 V3 치환**

진척바, 다음 버튼, 선택 카드 강조 등에서 보라/그라데이션 컬러 → `var(--primary)` / `var(--accent)` 치환.

- [ ] **Step 2: 1단계 5개 언어 카드 동등 처리**

모든 언어 카드를 동일 크기·동일 여백·동일 폰트 weight로. 선택된 카드만 `border: 2px solid var(--primary)` + `background: var(--primary-soft)`. 호버는 카드 살짝 떠오름(translateY -1px, shadow-md).

- [ ] **Step 3: 2단계 한국어 진단 문제 카드 V3**

문제 헤딩에 `font-family: var(--font-display)` (Fraunces) 적용. 옵션 카드는 흰 배경 + 얇은 border + radius-md.

- [ ] **Step 4: 3단계 자격증 선택 카드 V3**

자격증별 무지개 그라데이션 배경을 단색으로 변경. 자격증 아이콘 emoji는 유지(브랜드 친근감). "🌐 다국어 시험 가능" 배지는 `var(--accent)` 배경 + 흰 텍스트 작은 픽.

- [ ] **Step 5: 4단계 학습 플랜 요약 V3**

요약 박스의 보라 배경을 `var(--primary-soft)` 또는 `var(--bg-card)` + 액센트 보더로. "학습 시작하기!" CTA는 `var(--primary)` 솔리드.

- [ ] **Step 6: 브라우저 검증**

localStorage 비우고 처음부터:
```js
// 콘솔에서
localStorage.clear(); location.reload();
```

1단계 → 2단계 → 3단계 → 4단계 → 학습 시작까지 4스텝 다 V3 토큰인지, AI slop 잔재 없는지.

- [ ] **Step 7: 커밋**

```bash
git add src/components/pages/OnboardingPage.js
git commit -m "feat: OnboardingPage V3 — 평등 언어 카드 + 액센트 절제

- 5개 언어 카드 동등 크기·동등 무게 → 다양성 정체성 시각화
- 보라/무지개 그라데이션 → --primary 단색
- 자격증 카드 그라데이션 제거, 다국어 배지는 --accent로
- 진단 문제 헤딩에 Fraunces 디스플레이 폰트"
```

---

## Task 7: StudyPage V3 (핵심 화면)

**Files:**
- Modify: `src/components/pages/StudyPage.js`

이게 학생이 가장 오래 보는 화면. 디자인 일관성·가독성·정답 피드백 모두 V3 토큰으로.

- [ ] **Step 1: 진척바 색상 V3**

`1 / 15  정답률 X%` 진척바의 채움 색을 `var(--primary)` 또는 `linear-gradient(90deg, var(--primary), var(--accent))`로.

- [ ] **Step 2: 모드 탭(번역/힌트/실전) V3**

3개 탭 디자인:
- 비활성: `bg: transparent`, `color: var(--text-secondary)`
- 활성: `bg: var(--primary-soft)`, `color: var(--primary)`, `border-bottom: 2px solid var(--primary)`
- 라운드 sm (6px)

- [ ] **Step 3: 문제 헤딩 V3**

```jsx
<h2 style={{
    fontFamily: 'var(--font-display)',
    fontSize: 'clamp(20px, 4vw, 26px)',
    lineHeight: 1.4,
    color: 'var(--text-primary)',
    fontWeight: 600,
}}>{question}</h2>
```

STEP 라벨은 `var(--primary)` 작은 픽.

- [ ] **Step 4: 옵션 카드 V3**

기본 옵션 카드:
- `bg: var(--bg-card)` `border: 1px solid var(--border)` `radius: md` `padding: 16px 20px`
- 호버: `border-color: var(--primary-light)` `transform: translateY(-1px)` `box-shadow: var(--shadow-sm)`
- 번호 원: 28px 원, `bg: var(--primary-soft)` `color: var(--primary)` `font-weight: 600`

선택 시: `border: 2px solid var(--primary)` `bg: var(--primary-soft)`

정답 확인 후:
- 정답: `border: 2px solid var(--success)` `bg: var(--success-bg)`, 번호 원 → ✓ 아이콘
- 사용자가 고른 오답: `border: 2px solid var(--error)` `bg: var(--error-bg)`

- [ ] **Step 5: 정답 확인 CTA V3**

```jsx
<button style={{
    background: 'var(--primary)',
    color: '#fff',
    padding: '14px 24px',
    border: 'none',
    borderRadius: 'var(--radius-md)',
    fontFamily: 'var(--font-body)',
    fontSize: 16,
    fontWeight: 600,
    width: '100%',
    maxWidth: 320,
    cursor: 'pointer',
    transition: 'transform var(--dur-micro) var(--easing-move), background var(--dur-micro) var(--easing-move)',
}}>정답 확인 →</button>
```

호버 시 `background: var(--primary-light)`. 클릭 시 `transform: translateY(1px)`.

- [ ] **Step 6: 정답 해설 박스 V3**

정답 시:
```jsx
<div style={{
    background: 'var(--success-bg)',
    border: '1px solid var(--success)',
    borderRadius: 'var(--radius-md)',
    padding: 'var(--space-md)',
    marginTop: 'var(--space-md)',
}}>
    <h3 style={{margin:0,fontSize:14,color:'var(--success)'}}>🎉 정답이에요!</h3>
    <p style={{margin:'8px 0 0',fontSize:14,color:'var(--text-primary)',lineHeight:1.6}}>{explanation}</p>
</div>
```

오답 시 동일 패턴 + error 토큰.

- [ ] **Step 7: 다음 문제 → 버튼 V3**

CTA 와 동일 스타일. 또는 secondary 톤(`bg: transparent; border: 2px solid var(--primary); color: var(--primary)`).

- [ ] **Step 8: 브라우저 검증**

학습 진입 → 문제 풀이 → 정답/오답 모두 시나리오 확인. 헤딩이 Fraunces로 보이는지. 카드 라운드 8-12px인지(아주 둥글지 않은지). 

- [ ] **Step 9: 커밋**

```bash
git add src/components/pages/StudyPage.js
git commit -m "feat: StudyPage V3 — 핵심 화면 토큰화

- 문제 헤딩에 Fraunces 디스플레이 폰트
- 모드 탭(번역/힌트/실전) 활성 표시 --primary 언더라인
- 옵션 카드 4상태(기본/선택/정답/오답) 토큰화
- 정답/오답 해설 박스 V3 semantic 컬러
- CTA 보라 그라데이션 → 딥 틸 솔리드 + 호버 라이트 톤
- radius md(8px), 카드 호버 시 살짝 떠오름"
```

---

## Task 8: 남은 페이지 V3 — Licence + Dictionary + Community

**Files:**
- Modify: `src/components/pages/LicencePage.js`
- Modify: `src/components/pages/DictionaryPage.js`
- Modify: `src/components/pages/CommunityPage.js`

세 화면 모두 동일한 패턴(카드, 헤딩, 카테고리 픽)이라 묶어서 처리.

- [ ] **Step 1: LicencePage V3**

자격증 카드 6개의 그라데이션 배경 → 흰 배경 + 좌측 액센트 바(`var(--accent)`). "다국어 시험 가능" 배지는 `var(--accent)` 배경 흰 텍스트.

탭 (전체/조리/미용)은 픽 스타일 `var(--primary-soft)` 활성, `var(--text-secondary)` 비활성.

- [ ] **Step 2: DictionaryPage V3**

용어 카드:
- `bg: var(--bg-card)` `border: 1px solid var(--border)` `radius: md`
- 한국어 큰 글씨 — `font-family: var(--font-display)` (선택)
- 발음 `[ho-hwa]` — `font-family: var(--font-mono)` `color: var(--text-muted)`
- 번역 텍스트 본문 폰트

카테고리 픽(전체 과목/식품위생/식품학/조리이론)은 V3 픽 스타일.

검색 입력 — `border: 1px solid var(--border)` `radius: sm` `focus border 2px var(--primary)`.

- [ ] **Step 3: CommunityPage V3**

합격 수기 카드 — 흰 배경, 인용구 스타일에 좌측 액센트 바(`var(--accent)`).

공부 꿀팁 6개 카드 — 통일된 톤. 무지개 4색 띠 있다면 제거.

유용한 링크 카드 — 외부 링크 작은 픽 형태.

- [ ] **Step 4: 브라우저 검증**

3개 탭 다 돌아다니면서:
1. 보라 잔재 없는지
2. 자격증/용어/꿀팁 카드가 일관된 톤인지
3. 무지개 색 띠 흔적 없는지

- [ ] **Step 5: 커밋**

```bash
git add src/components/pages/LicencePage.js src/components/pages/DictionaryPage.js src/components/pages/CommunityPage.js
git commit -m "feat: Licence/Dictionary/Community V3 적용

- 카드 그라데이션 → 흰 배경 + 좌측 액센트 바
- 다국어 배지, 카테고리 픽 V3 토큰화
- 사전: 한글 디스플레이 폰트 + 발음 모노 폰트
- 합격 수기: 인용 좌측 액센트 바
- 무지개 4색 띠 흔적 모두 제거"
```

---

## Task 9: 한식조리기능사 콘텐츠 60문제 + 40용어

**Files:**
- Modify: `src/data/questions/korean-food.js` (15 → 60문제로 확장)
- Modify: `src/data/terms/korean-food.js` (23 → 40용어로 확장)

자격증 출제 범위 단원:
- 식품위생 및 법규: 20문제 (subject: `food-hygiene`)
- 식품학: 20문제 (`food-science`)
- 조리이론 및 원가계산: 20문제 (`cooking-theory`)
공중보건학은 0문제 유지(스펙 따름).

저작권 정책: Q-net 기출문제 그대로 복사 ❌. 기출 패턴·주제·난이도 분석 후 변형 작성.

- [ ] **Step 1: 출제 범위 매핑 — 빈출 주제 정리**

다음 정보를 문서로 정리 (작성자 본인용 참고):
- 식품위생 및 법규 빈출 주제: HACCP, 식품위생법, 식중독 원인균(살모넬라·포도상구균·보툴리누스·노로바이러스), 식품첨가물(보존료·발색제), 표시광고법, 위생관리, 영업허가
- 식품학 빈출 주제: 5대 영양소(탄수화물·단백질·지방·비타민·무기질), 갈변현상(효소적/비효소적), 발효(젖산균·효모), 식품 변질
- 조리이론 빈출 주제: 조리법 분류(습열/건열), 한식 기본조리(찜·볶음·구이·무침), 양념장 비율, 육수, 칼질, 원가계산(재료비 산출)

- [ ] **Step 2: 기존 15문제 검토 + 단원별 분포 채우기**

`src/data/questions/korean-food.js`를 열고 기존 15문제의 subject 분포 확인:
```bash
grep "subject:" src/data/questions/korean-food.js | sort | uniq -c
```

목표: 단원별 20문제씩. 추가 45문제를 단원별 빈도 맞춰 작성.

- [ ] **Step 3: 문제 한 개당 다음 6필드 모두 채워서 작성**

각 문제 객체:
```js
{
    id: 'kf-XX',                    // 한식 XX번
    licenceId: 'korean-food',
    subject: 'food-hygiene' | 'food-science' | 'cooking-theory',
    question: '...',                // 시험 원문 스타일 (정확하고 격식 있는 한국어)
    simpleQuestion: '...',          // 쉬운 한국어 풀어쓰기 (다문화 학생 1차 사용자용)
    options: ['...', '...', '...', '...'],  // 4개 선택지
    correctAnswer: 0|1|2|3,         // index (0-based)
    explanation: '...',             // 시험 원문 해설
    simpleExplanation: '...',       // 쉬운 한국어 해설
    keywords: ['...', '...', ...],  // 태그용 키워드 2-4개
}
```

특히 `simpleQuestion`/`simpleExplanation`은 다문화 학생이 이해할 수 있는 쉬운 한국어로. 예:
- question: "식품위생법 제○조에 따른 표시사항이 아닌 것은?"
- simpleQuestion: "음식 포장에 꼭 써야 하는 것이 아닌 것은?"

- [ ] **Step 4: 60문제 작성 완료 후 정합성 검증**

```bash
grep -c "id: 'kf-" src/data/questions/korean-food.js
# Expected: 60
grep "subject:" src/data/questions/korean-food.js | sort | uniq -c
# Expected: 20 food-hygiene, 20 food-science, 20 cooking-theory
```

ID는 `kf-01` ~ `kf-60` 순서. 중복·누락 없는지.

- [ ] **Step 5: 용어 사전 40개로 확장**

`src/data/terms/korean-food.js`를 열고 23개에서 40개로 추가:
- 식품위생 카테고리: 6 → 10 (추가: 식품첨가물·HACCP 12원칙·살균/멸균 차이·식품안전관리인증)
- 식품학 카테고리: 9 → 14 (추가: 무기질·아미노산·5대 영양소·열량 계산·식이섬유)
- 조리이론 카테고리: 8 → 16 (추가: 양념장 종류·기름 종류·칼 종류·온도조절·시간조절·계량·플레이팅·전·찌개·국)

각 용어 객체:
```js
{
    korean: '한국어',
    pronunciation: 'han-guk-eo',
    category: '식품위생' | '식품학' | '조리이론',
    vi: 'Tiếng Việt',
    zh: '中文',
    th: 'ภาษาไทย',
    tl: 'Tagalog',
    my: 'မြန်မာဘာသာ',
}
```

작성 정책: vi·zh는 정확하게 (사용 빈도 큼). th·tl·my는 빈 문자열 가능 (Task 12에서 정책 적용).

- [ ] **Step 6: 검증**

```bash
grep -c "korean:" src/data/terms/korean-food.js
# Expected: 40

npm run dev
```
브라우저에서 학습 진입 → 한식조리 → 문제 풀이로 60문제 끝까지 진행되는지 확인. 사전 탭에서 한식조리 필터 → 40개 보이는지.

- [ ] **Step 7: 테스트로 콘텐츠 적합성 가드**

`src/data/__tests__/korean-food.test.js`:
```js
import { describe, it, expect } from 'vitest';
import { koreanFoodQuestions } from '../../data/questions/korean-food';
import { koreanFoodTerms } from '../../data/terms/korean-food';

describe('한식조리기능사 콘텐츠', () => {
    it('문제 60개', () => {
        expect(koreanFoodQuestions).toHaveLength(60);
    });
    it('모든 문제에 6필드 다 있음', () => {
        for (const q of koreanFoodQuestions) {
            expect(q.id).toMatch(/^kf-\d{2}$/);
            expect(q.licenceId).toBe('korean-food');
            expect(['food-hygiene', 'food-science', 'cooking-theory']).toContain(q.subject);
            expect(q.question).toBeTruthy();
            expect(q.simpleQuestion).toBeTruthy();
            expect(q.options).toHaveLength(4);
            expect([0, 1, 2, 3]).toContain(q.correctAnswer);
            expect(q.explanation).toBeTruthy();
            expect(q.simpleExplanation).toBeTruthy();
            expect(Array.isArray(q.keywords)).toBe(true);
        }
    });
    it('단원별 20문제씩', () => {
        const bySubject = koreanFoodQuestions.reduce((acc, q) => {
            acc[q.subject] = (acc[q.subject] || 0) + 1;
            return acc;
        }, {});
        expect(bySubject['food-hygiene']).toBe(20);
        expect(bySubject['food-science']).toBe(20);
        expect(bySubject['cooking-theory']).toBe(20);
    });
    it('용어 40개 + 5언어 필드 있음', () => {
        expect(koreanFoodTerms).toHaveLength(40);
        for (const t of koreanFoodTerms) {
            expect(t.korean).toBeTruthy();
            expect(t.pronunciation).toBeTruthy();
            expect(t.category).toBeTruthy();
            expect(t).toHaveProperty('vi');
            expect(t).toHaveProperty('zh');
            expect(t).toHaveProperty('th');
            expect(t).toHaveProperty('tl');
            expect(t).toHaveProperty('my');
        }
    });
});
```

```bash
npm run test:run
```
Expected: PASS

- [ ] **Step 8: 커밋**

```bash
git add src/data/questions/korean-food.js src/data/terms/korean-food.js src/data/__tests__/korean-food.test.js
git commit -m "content: 한식조리기능사 60문제 + 40용어 완성

- 기존 15문제 → 60문제 (단원별 20씩: 식품위생/식품학/조리이론)
- 기존 23용어 → 40용어 (단원별 분포 보강)
- 모든 문제에 simpleQuestion/simpleExplanation 필드 채움
- 5개 언어 필드 vi/zh 정확하게, th/tl/my 우선 빈 값 (Task 12에서 정책 적용)
- 콘텐츠 가드 테스트 추가 (개수·필드·단원 분포)
- 출제 출처: Q-net 공개 기출 패턴 + 시중 교재 분석 후 변형 작성"
```

---

## Task 10: 미용사(일반) 콘텐츠 60문제 + 40용어

**Files:**
- Modify: `src/data/questions/beauty-general.js` (10 → 60문제로 확장)
- Modify: `src/data/terms/beauty-general.js` (12 → 40용어로 확장)

자격증 출제 범위 단원 (`licences.js` 기준):
- 두피·모발: 15문제 (`hair-theory`)
- 화장품학: 15문제 (`cosmetic`)
- 공중위생: 15문제 (`beauty-hygiene`)
- 미용 이론: 15문제 (`beauty-theory`)

- [ ] **Step 1: 출제 범위 매핑 — 단원별 빈출 주제 정리**

- 두피·모발: 모발 구조(피질·수질·표피), 모발 성장 단계(성장기·퇴행기·휴지기), 두피 상태 분류, 모근·모간, 멜라닌
- 화장품학: 화장품 분류(기초·메이크업·바디·헤어), 화장품 성분(보습제·계면활성제·방부제), 화장품 안전성, 표시광고기준
- 공중위생: 미용실 위생기준, 미용 용구 소독법(자비·증기·자외선), 미용업 영업신고, 면허·자격, 위생교육
- 미용 이론: 미용의 정의, 헤어 디자인 원리, 헤어컬링·세팅·아이론, 모발 손상 원인, 두발화장품 사용

- [ ] **Step 2: 60문제 작성**

ID 형식: `bg-01` ~ `bg-60`. 각 단원 15문제씩.

기존 10문제 그대로 유지하고 50문제 추가. 모든 문제에 6필드 채움 (Task 9 Step 3 참조).

- [ ] **Step 3: 용어 40개 작성**

`src/data/terms/beauty-general.js`. 12개 → 40개 (단원별 10개씩). 5언어 필드 vi/zh 정확하게, 나머지 빈 값.

- [ ] **Step 4: 콘텐츠 가드 테스트**

`src/data/__tests__/beauty-general.test.js`:
```js
import { describe, it, expect } from 'vitest';
import { beautyGeneralQuestions } from '../../data/questions/beauty-general';
import { beautyGeneralTerms } from '../../data/terms/beauty-general';

describe('미용사(일반) 콘텐츠', () => {
    it('문제 60개', () => {
        expect(beautyGeneralQuestions).toHaveLength(60);
    });
    it('단원 4개 × 15문제', () => {
        const bySubject = beautyGeneralQuestions.reduce((acc, q) => {
            acc[q.subject] = (acc[q.subject] || 0) + 1;
            return acc;
        }, {});
        ['hair-theory', 'cosmetic', 'beauty-hygiene', 'beauty-theory'].forEach(s => {
            expect(bySubject[s]).toBe(15);
        });
    });
    it('필수 필드 검증', () => {
        for (const q of beautyGeneralQuestions) {
            expect(q.id).toMatch(/^bg-\d{2}$/);
            expect(q.licenceId).toBe('beauty-general');
            expect(q.options).toHaveLength(4);
            expect(q.simpleQuestion).toBeTruthy();
            expect(q.simpleExplanation).toBeTruthy();
        }
    });
    it('용어 40개', () => {
        expect(beautyGeneralTerms).toHaveLength(40);
    });
});
```

```bash
npm run test:run
```
Expected: PASS (한식조리 + 미용 둘 다)

- [ ] **Step 5: 브라우저 검증**

학습 진입 → 자격증 변경 → 미용사(일반) → 60문제 진행. 사전에서 미용 필터 → 40개.

- [ ] **Step 6: 커밋**

```bash
git add src/data/questions/beauty-general.js src/data/terms/beauty-general.js src/data/__tests__/beauty-general.test.js
git commit -m "content: 미용사(일반) 60문제 + 40용어

- 단원별 15문제(두피·모발/화장품학/공중위생/미용 이론)
- 용어 12 → 40 확장
- 콘텐츠 가드 테스트 추가"
```

---

## Task 11: 제과기능사 콘텐츠 60문제 + 40용어 (신규)

**Files:**
- Modify: `src/data/questions/pastry.js` (0 → 60문제)
- Modify: `src/data/terms/pastry.js` (0 → 40용어)

자격증 출제 범위 단원:
- 과자류 재료: 20문제 (`pastry-theory`)
- 과자류 제조: 20문제 (`pastry-process`)
- 식품위생학: 20문제 (`pastry-hygiene`)

- [ ] **Step 1: 출제 범위 매핑**

- 과자류 재료: 밀가루(강력·중력·박력), 유지(버터·마가린·쇼트닝), 당류(설탕·물엿·꿀), 계란, 유제품, 팽창제(베이킹파우더·이스트), 초콜릿, 향료
- 과자류 제조: 반죽 방법(크림법·블렌딩법·1단계법), 굽기(오븐 온도·시간), 데코레이션, 보관, 제품별 특성(쿠키·케이크·도넛·파이)
- 식품위생학: 식품위생 일반, 식중독, 위생관리, HACCP, 첨가물

- [ ] **Step 2: 60문제 작성**

ID 형식: `ps-01` ~ `ps-60`. 단원별 20문제. 6필드 모두 채움.

- [ ] **Step 3: 40용어 작성**

5언어 필드 vi/zh 정확하게.

- [ ] **Step 4: 가드 테스트**

`src/data/__tests__/pastry.test.js`:
```js
import { describe, it, expect } from 'vitest';
import { pastryQuestions } from '../../data/questions/pastry';
import { pastryTerms } from '../../data/terms/pastry';

describe('제과기능사 콘텐츠', () => {
    it('문제 60개', () => {
        expect(pastryQuestions).toHaveLength(60);
    });
    it('단원 3개 × 20문제', () => {
        const bySubject = pastryQuestions.reduce((acc, q) => {
            acc[q.subject] = (acc[q.subject] || 0) + 1;
            return acc;
        }, {});
        ['pastry-theory', 'pastry-process', 'pastry-hygiene'].forEach(s => {
            expect(bySubject[s]).toBe(20);
        });
    });
    it('필수 필드 검증', () => {
        for (const q of pastryQuestions) {
            expect(q.id).toMatch(/^ps-\d{2}$/);
            expect(q.licenceId).toBe('pastry');
            expect(q.options).toHaveLength(4);
            expect(q.simpleQuestion).toBeTruthy();
            expect(q.simpleExplanation).toBeTruthy();
        }
    });
    it('용어 40개', () => {
        expect(pastryTerms).toHaveLength(40);
    });
});
```

```bash
npm run test:run
```
Expected: PASS

- [ ] **Step 5: 브라우저 검증**

홈 → 자격증 → 제과기능사 클릭 → 학습 화면에서 60문제 진행. 사전에서 제과 추가.

- [ ] **Step 6: 커밋**

```bash
git add src/data/questions/pastry.js src/data/terms/pastry.js src/data/__tests__/pastry.test.js
git commit -m "content: 제과기능사 60문제 + 40용어 (신규)

- 단원별 20문제(과자류 재료/제조/식품위생학)
- 신규 용어 40개 작성
- 콘텐츠 가드 테스트 추가
- 3개 자격증 콘텐츠 모두 60+40 충실 달성"
```

---

## Task 12: 5언어 정책 + 시연 친화 + 메타태그

**Files:**
- Modify: `src/components/pages/DictionaryPage.js` (5언어 정책)
- Modify: `src/components/pages/OnboardingPage.js` (5언어 정책)
- Modify: `src/components/layout/TopNav.js` (데모 초기화 단축키)
- Modify: `src/app/layout.js` (메타태그)
- Create: `public/og-image.png` (선택 — 없으면 메타만 정리)

5언어 정책: vi/zh 검수 완료, th/tl/my는 "준비 중"으로 표시. 데모 초기화 단축키: 로고 5회 클릭. 메타태그: title, description, OG.

- [ ] **Step 1: 5언어 정책 헬퍼 함수 작성**

`src/lib/demoMode.js`에 추가:
```js
// 검수 완료된 언어
const VERIFIED_LANGS = ['vi', 'zh'];

export function isLanguageVerified(langCode) {
    return VERIFIED_LANGS.includes(langCode);
}

export function getLanguageStatus(langCode) {
    return VERIFIED_LANGS.includes(langCode) ? 'verified' : 'preview';
}
```

- [ ] **Step 2: DictionaryPage에서 미검수 언어 처리**

용어 카드에서 미검수 언어의 번역이 빈 값이면 "🚧 준비 중" 표시:
```jsx
import { isLanguageVerified } from '@/lib/demoMode';

// 용어 카드에서:
{LANG_CODES.map(code => (
    <div key={code} className="term-translation">
        <span className="lang-label">{LANG_LABELS[code]}</span>
        {term[code] ? (
            <span>{term[code]}</span>
        ) : (
            <span className="preview">🚧 준비 중</span>
        )}
    </div>
))}
```

CSS:
```css
.preview {
    color: var(--text-muted);
    font-size: 12px;
    font-style: italic;
}
```

- [ ] **Step 3: OnboardingPage 1단계에서 미검수 언어 배지**

vi/zh 외의 언어 카드에 작은 "Coming soon" 배지:
```jsx
{!isLanguageVerified(lang.code) && (
    <span style={{
        position: 'absolute', top: 8, right: 8,
        background: 'var(--accent-soft)',
        color: 'var(--text-secondary)',
        fontSize: 10,
        padding: '2px 6px',
        borderRadius: 'var(--radius-full)',
    }}>Coming soon</span>
)}
```

사용자가 미검수 언어 선택해도 진행은 가능 (학습 모드에선 한국어로 보임). 단지 사전 번역이 일부 비어있을 뿐.

- [ ] **Step 4: 데모 초기화 단축키 (로고 5회 클릭)**

`src/components/layout/TopNav.js`에 추가:
```jsx
const [logoClicks, setLogoClicks] = useState(0);
const [clickTimer, setClickTimer] = useState(null);

function handleLogoClick() {
    setLogoClicks(c => c + 1);
    if (clickTimer) clearTimeout(clickTimer);
    setClickTimer(setTimeout(() => setLogoClicks(0), 1500));

    if (logoClicks + 1 >= 5) {
        if (confirm('데모 초기화하시겠어요? 모든 학습 데이터가 지워집니다.')) {
            localStorage.removeItem('rainbow_study');
            location.reload();
        }
        setLogoClicks(0);
    }
}

// 워드마크에 onClick={handleLogoClick} 부착
```

- [ ] **Step 5: 메타태그 + OG 정리**

`src/app/layout.js`의 `metadata` 객체:
```js
export const metadata = {
    title: 'Rainbow Licence — 다문화 학생을 위한 한국 자격증 학습',
    description: '한식조리·미용·제과 등 한국 국가기술자격증을 5개 언어로 학습. 다문화 학교 NEXT SCHOOL과 함께.',
    openGraph: {
        title: 'Rainbow Licence',
        description: '다문화 학생을 위한 한국 자격증 학습 도구',
        type: 'website',
        locale: 'ko_KR',
        siteName: 'Rainbow Licence',
    },
    twitter: {
        card: 'summary_large_image',
        title: 'Rainbow Licence',
        description: '다문화 학생을 위한 한국 자격증 학습 도구',
    },
    icons: {
        icon: '/favicon.ico',
    },
};
```

(OG 이미지는 일단 텍스트 메타만. 이미지는 M2에서 추가 가능.)

- [ ] **Step 6: 브라우저 검증**

1. 사전에서 한식조리 필터 → 한 용어 클릭/확장 시 th/tl/my가 "🚧 준비 중"으로 보이는지
2. 온보딩 1단계에서 vi/zh 외 카드에 "Coming soon" 배지 보이는지
3. 로고 5회 빨리 클릭 → 확인 다이얼로그 → 확인 누르면 localStorage 비우고 새로고침
4. `view-source:` 또는 메타 검사 도구로 title/description 확인

- [ ] **Step 7: 커밋**

```bash
git add src/lib/demoMode.js src/components/pages/DictionaryPage.js src/components/pages/OnboardingPage.js src/components/layout/TopNav.js src/app/layout.js
git commit -m "feat: 5언어 정책 + 데모 초기화 단축키 + 메타태그

- 5언어 정책: vi/zh 검수 완료 노출, th/tl/my는 '🚧 준비 중' 표시
- 온보딩: 미검수 언어 카드에 'Coming soon' 배지 (진행은 가능)
- TopNav: 로고 5회 클릭 → 데모 초기화 (확인 다이얼로그)
- 메타태그: title/description/OG 정리"
```

---

## Task 13: Vercel 배포

**Files:**
- Verify: `package.json`, `next.config.mjs`
- Create: `.vercelignore` (선택)

수동 단계 일부 포함 (Vercel CLI 로그인 등). 평가위원·교장선생님께 공유할 URL 확보가 목표.

- [ ] **Step 1: 프로덕션 빌드 로컬 검증**

```bash
cd /Volumes/AIPART/dev/rainbow-licence
npm run build
```
Expected: 빌드 성공, 에러 없음. 경고는 OK.

```bash
npm run start
```
브라우저에서 http://localhost:3000 (또는 자동 할당된 포트) 열어 프로덕션 빌드 동작 확인.

- [ ] **Step 2: Vercel CLI 설치 + 로그인 (있으면 스킵)**

```bash
npm i -g vercel
vercel login
```
브라우저 로그인 진행.

- [ ] **Step 3: 첫 배포 (preview)**

```bash
vercel
```
프롬프트 응답:
- Set up and deploy? Y
- Which scope? (사용자 계정 선택)
- Link to existing project? N
- Project name? `rainbow-licence`
- Directory? `./` (현재 위치)
- Override settings? N (Next.js 자동 감지)

배포 완료되면 preview URL 출력됨 (예: `rainbow-licence-xxx.vercel.app`).

- [ ] **Step 4: Vercel 환경변수 설정**

Vercel 대시보드 또는 CLI:
```bash
vercel env add NEXT_PUBLIC_DEMO_MODE
# 값: true
# Environment: Production, Preview, Development 다 선택

vercel env add NEXT_PUBLIC_SCHOOL_NAME
# 값: NEXT SCHOOL
# Environment: 다 선택
```

- [ ] **Step 5: 프로덕션 배포**

```bash
vercel --prod
```
프로덕션 URL 발급 (예: `rainbow-licence.vercel.app`).

- [ ] **Step 6: 배포된 사이트 QA 체크리스트**

브라우저로 프로덕션 URL 열어 다음 확인:
- [ ] 첫 로딩 — 폰트가 Pretendard로 보이는가 (FOUT 잠시 있어도 OK)
- [ ] 온보딩 4단계 다 진행 가능한가
- [ ] 학교명 "NEXT SCHOOL" 헤더에 표시되는가
- [ ] 5언어 도트 헤더에 보이는가
- [ ] "Coming soon" 배지가 th/tl/my 카드에 보이는가
- [ ] 학습 화면에서 한식조리 60문제 끝까지 진행 가능한가
- [ ] 미용일반·제과도 진행 가능한가
- [ ] 사전에서 3개 자격증 다 보이고, 미검수 언어 "준비 중" 표시되는가
- [ ] PremiumBanner 자리에 "기관 도입 문의" 카드 보이는가
- [ ] 로고 5회 클릭 → 데모 초기화 동작하는가
- [ ] 모바일 viewport (Chrome DevTools)에서 정상 작동하는가
- [ ] 데스크탑 1440px에서 사이드바 + 메인 + 우측 통계 레이아웃 보이는가
- [ ] 콘솔에 에러 없는가
- [ ] 옛 localStorage 시나리오 — `localStorage.setItem('rainbow_study', '{"user":{}}')` 후 새로고침 → 앱 안 죽는가

- [ ] **Step 7: 커밋 + URL 기록**

```bash
git add .
git commit -m "docs: Vercel 배포 완료

배포 URL:
- Production: https://rainbow-licence.vercel.app
- 환경변수: NEXT_PUBLIC_DEMO_MODE=true, NEXT_PUBLIC_SCHOOL_NAME=NEXT SCHOOL

M1 시연 베타 완료. 교장선생님 시연 + 공공 평가 자료화 단계 진입 가능." --allow-empty
```

(또는 spec 문서에 URL 기록)

---

## 완료 정의 (Definition of Done)

M1 시연 베타 완료 조건:
- ✅ 모든 Task 1-13 체크박스 완료
- ✅ `npm run test:run` 통과 (콘텐츠 가드 테스트 3개 자격증 포함)
- ✅ `npm run build` 통과
- ✅ Vercel 프로덕션 URL 접근 가능
- ✅ Task 13 Step 6의 QA 체크리스트 전부 통과
- ✅ 옛 localStorage 시나리오에서 앱 안 죽음
- ✅ 보라 그라데이션·풀무지개 흔적 없음
- ✅ DEMO_MODE=true 환경에서 ₩9,900/월 결제 UI 노출 안 됨
- ✅ 학교명 "NEXT SCHOOL" 헤더에 노출
- ✅ 3개 자격증 각 60문제+40용어 충실
- ✅ 5언어 정책 적용 (vi/zh 검수, 나머지 준비중)
